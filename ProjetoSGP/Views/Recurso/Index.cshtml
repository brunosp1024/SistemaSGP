@model IEnumerable<ProjetoSGP.Models.Recurso>

@{
    ViewBag.Title = "Index";
}

<h2 class="titulo-lista">Lista dos Recursos</h2>
<p>
    @Html.ActionLink(" ", "Create", new { }, new { @class = "btn btn-success glyphicon glyphicon-plus add" })
</p>

<div class="tabela">
    <table class="table table-condensed table-hover">
        <tr class="cor-titulo">
            <th>
                @Html.DisplayNameFor(model => model.IdRecurso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefone)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdRecurso)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Telefone)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.IdRecurso }, new { @class = "btn btn-primary glyphicon glyphicon-edit" })
                    @Html.ActionLink("Detalhes", "Details", new { id = item.IdRecurso }, new { @class = "btn btn-warning glyphicon glyphicon-file" })
                    @Html.ActionLink("Excluir", "Delete", new { id = item.IdRecurso }, new { @class = "btn btn-danger glyphicon glyphicon-trash" })
                </td>
            </tr>
        }

    </table>
</div>

<hr />

<div style="text-align:center">
    <button class="btn btn-default">@Html.ActionLink("Voltar", "Index", "Home")</button>
</div>

<style>

    .add {
        margin-bottom: -50px;
    }

    .tabela {
        width: 100%;
        height: 380px;
        overflow: hidden;
        overflow-y: scroll;
        margin: 40px auto -5px auto;
    }

    tr th, tr td {
        color: black;
        text-align: left;
    }


    .flutuar {
        float: left;
        margin-bottom: 5px;
        position: relative;
        top: 50px;
    }

    .titulo-lista {
        position: relative;
        margin-right: 60px;
        color: black;
        text-align: center;
    }

    a {
        text-decoration: none;
    }

    .cor-titulo {
        background: #F2F2F2;
        pointer-events: none;
    }
</style>